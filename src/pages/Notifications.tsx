import { Layout } from "@/components/layout/Layout";

export default function Notifications() {
  return (
    <Layout>
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold text-foreground mb-4">Configurar Notificaciones</h1>
        <p className="text-muted-foreground">Personalizá las alertas que querés recibir.</p>
        {/* Notifications content will be implemented */}
      </div>
    </Layout>
  );
}